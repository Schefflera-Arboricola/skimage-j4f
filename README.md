# skimage backend - just for fun

```
python3 -m venv dev-env
source dev-env/bin/activate
pip install git+https://github.com/Schefflera-Arboricola/scikit-image.git@patch-1
pip install -e .
pip install git+https://github.com/betatim/scikit-image-backend-phony.git@main
python3 rough.py
```
